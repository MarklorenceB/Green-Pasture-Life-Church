Drop the church's donation QR code images here:

  gcash-qr.png   — GCash QR code
  bdo-qr.png     — BDO QR code

Square images (e.g. 600x600) work best. Until these files exist, the
Give page shows a "QR code coming soon" placeholder automatically.

Also update the account name/number in src/data/mockdata.js (givingMethods).
